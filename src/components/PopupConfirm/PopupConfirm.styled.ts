import styled from "styled-components";

export const ConfirmContainer = styled.div({
  alignItems: "center",
  display: "flex",
  justifyContent: "center",
  flexDirection: "column",
});
export const TitlePopup = styled.h2({
  fontSize: "1.3rem",
  marginTop: 10,
  marginBottom: 10,
  fontWeight: 600,
  textAlign: "center",
});
export const DescriptionPopup = styled.span({
  fontSize: "1rem",
  marginBottom: 10,
  textAlign: "center",
});

export const ContentFooter = styled.div({
  display: "flex",
  width: "100%",
  marginTop: 10,
  justifyContent: "center",
});

export const WrapperBtn = styled.div({
  width: "50%",
  padding: "0 10px",
});
